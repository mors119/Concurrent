Thread
→ std::thread::spawn

Join
→ JoinHandle.join()

Mutex
→ std::sync::Mutex

Semaphore
→ tokio::sync::Semaphore

Atomic
→ AtomicUsize / AtomicBool

CAS
→ compare_exchange()

Channel
→ std::sync::mpsc

Thread Pool
→ rayon / tokio

Async
→ async/await + tokio
